meta:
  environment: node
  toolchain: npm
  version: "20"
build:
  skip: false
  additionalCommands: npm install && npm run build
run:
  command: npm run startProd
  persistenceMount: /data
  containerPort: "80"
